# Natural-language demo tooling — **DEFERRED / ON HOLD**

The historical **`docgen demo-function`** pipeline and related Playwright-test capture surface have been **removed** from this library. A follow-on design (informally sketched as NL goals → automation spec → timed narration) was **not shipped** and is **on hold**.

When work resumes, expect the approach to be **revisited from scratch** against whatever video and demo contracts the project needs then. This file intentionally does **not** point at prototype paths or promise files that are not part of the current tree.

# Terminal demos (removed)

The **VHS / `.tape`** terminal recording path was **removed** from `docgen` (same removal window as Playwright / demo-function). This directory is kept only so older links to `docs/demos/terminal/` do not 404; **no tapes** are used.

See root **`AGENTS.md`** and **`docs/demos/README.md`**.

#!/usr/bin/env bash
# Historic: `docgen per-function-render` / Playwright capture was removed from the library.
# See root AGENTS.md. This stub remains so old notes that mention the script name fail loudly with a clear message.
set -euo pipefail
echo "[_rebuild-per-function] per-function and demo-function pipelines were removed from docgen." >&2
echo "  Use Manim + docgen generate-all from this bundle instead." >&2
exit 2

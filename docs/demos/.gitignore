# docgen: intermediate objects (regenerable)
.venv/
slides/node_modules/
slides/dist/
animations/media/
terminal/rendered/.tmp/
terminal/rendered/*.mp4
audio/*.mp3
__pycache__/
*.pyc
.docgen-state.json

# recordings/*.mp4 — committed for dogfood demos / README links (large files — consider Git LFS).
